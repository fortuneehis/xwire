export enum Codes {
  REQUEST_ALREADY_PROCESSED = 200191,
  INVALID_REQUEST_FORMAT = 400258,
  INVALID_TOKEN = 401136,
  INSUFFICIENT_FUNDS = 422419,
  INVALID_TAG_OR_PASSWORD = 401991,
  DUPLICATE_IDEMPOTENCY_KEY = 400853,
  ENTITY_NOT_FOUND = 404579,
}
